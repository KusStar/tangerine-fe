import React from 'react';
import { Grid } from '@material-ui/core';
import HeaderBar from '@/components/HeaderBar';
 
const Home: React.FC = () => {
  return (
    <Grid>
      <HeaderBar />
    </Grid>
  );
}

export default Home;